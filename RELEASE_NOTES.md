- Keychange can now be installed with Homebrew: `brew tap dennistimmermann/tap` followed by
  `brew install --cask keychange`.
- The menu bar mark now shows a pause symbol instead of an exclamation mark while Keychange is
  standing aside after you switched the input source yourself.
